import { Inter, REM, Roboto } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const roboto = Roboto({ weight: "400", subsets: ["latin"] });
export const rem = REM({ weight: "400", subsets: ["latin"] });
